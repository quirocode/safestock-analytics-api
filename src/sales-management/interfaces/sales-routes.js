const express=require('express');class SalesRoutes{constructor({controller,authentication}){this.router=express.Router();this.router.use(authentication.authenticate);this.router.post('/',controller.register);this.router.get('/',controller.list);this.router.put('/:id/anular',authentication.authorize('ADMIN'),controller.cancel);this.router.get('/:id/comprobante',controller.receipt);}}module.exports=SalesRoutes;
